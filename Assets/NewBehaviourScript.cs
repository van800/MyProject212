using System.Collections;
using System.Collections.Generic;
using ClassLibrary26;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Class1.Test();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
